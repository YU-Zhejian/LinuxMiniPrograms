#!/usr/bin/env bash
# AD2U V2
mypy=$(cat ${PWD}/../../etc/python.conf)
if [ -x "${mypy}" ];then
    "${mypy}" "${PWD}"/../exec/adoc2usage.py "${@}"
else
   echo -e "\e[033mERROR: Python ${mypy} invalid!\e[0m"
   exit 1
fi
