#!/usr/bin/env bash
# YLMKTNL V4P1
DN=$(dirname "${0}")
mypy=$(cat ${DN}/../etc/python.conf)
. "${DN}"/../etc/path.sh
if [ "${mypython}" != "ylukh" ];then
    "${mypython}" "${DN}"/exec/ylmktbl.py "${PWD}" "${@}"
else
   . "${DN}"/exec/ylmktbl.sh
fi
