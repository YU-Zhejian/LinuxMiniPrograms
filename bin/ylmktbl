#!/usr/bin/env bash
VERSION=4.1
set -ue
DN="$(readlink -f "$(dirname "${0}")")"
. "${DN}"/../etc/path.conf
if [ "${mypython}" != "ylukh" ]; then
	exec "${mypython}" "${DN}"/exec/ylmktbl.py "${@}"
else
	. "${DN}"/exec/ylmktbl.sh
fi
