#!/usr/bin/env bash
# YLMKTNL V4P1
set -ue
DN="$(readlink -f "$(dirname "${0}")")"
. "${DN}"/../etc/path.sh
[ "${mypython}" != "ylukh" ] && "${mypython}" "${DN}"/exec/ylmktbl.py "${PWD}" "${@}" || . "${DN}"/exec/ylmktbl.sh
