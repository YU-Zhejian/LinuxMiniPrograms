#!/usr/bin/env bash
# YLMKTNL V3P2
DN=$(dirname "${0}")
mypy=$(cat ${DN}/../etc/python.conf)
if [ -x "${mypy}" ];then
    "${mypy}" "${DN}"/exec/ylmktbl.py "${@}"
else
   . "${DN}"/exec/ylmktbl.sh
fi
