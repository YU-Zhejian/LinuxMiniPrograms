#!/usr/bin/env bash
# B16C v1
set -ue
DN="$(readlink -f "$(dirname "${0}")")"
. "${DN}"/../lib/libstr
. "${DN}"/../etc/path.sh
mkdir -p "${DN}"/var/enigma.d
if [ "${mypython}" != "ylukh" ]; then "${mypython}" "${DN}"/exec/b16c.py "${DN}"/../ "${@}"; else errh "No python found!"; fi
