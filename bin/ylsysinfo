#!/usr/bin/env bash
# YLSYSINFO V1
DN="$(readlink -f "$(dirname "${0}")")"
set -ue

. "${DN}"/../etc/path.sh
if [ "${mypython}" != "ylukh" ];then
    "${mypython}" "${DN}"/exec/ylsysinfo.py "${PWD}" "${@}"
else
   . "${DN}"/exec/ylsysinfo.sh
fi
