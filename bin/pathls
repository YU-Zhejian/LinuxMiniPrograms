#!/usr/bin/env bash
# shellcheck disable=SC2034
VERSION=3.5
builtin set -eu
DN="$(readlink -f "$(dirname "${0}")")"
. "${DN}"/../shlib/libinclude.sh
__include libstr
infoh "YuZJLab PATH ls"
infoh "Copyright (C) 2019-2021 YU Zhejian"
. "${DN}"/../etc/path.conf
# shellcheck disable=SC2154
if [ "${mypython}" != "ylukh" ]; then
    builtin exec "${mypython}" "${DN}"/exec/pathls.py "${@}"
else
    . "${DN}"/exec/pathls.sh
fi
