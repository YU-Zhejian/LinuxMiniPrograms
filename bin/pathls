#!/usr/bin/env bash
# PLS V3P2
set -ue
DN="$(readlink -f "$(dirname "${0}")")"
infoh "YuZJLab PATH ls"
infoh "Copyright (C) 2019-2020 YU Zhejian"
. "${DN}"/../etc/path.sh
[ "${mypython}" != "ylukh" ] && "${mypython}" "${DN}"/exec/pathls.py "${PWD}" "${@}" || . "${DN}"/exec/pathls.sh
