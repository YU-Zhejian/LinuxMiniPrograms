#!/usr/bin/env bash
# LIBDOMAN V6
set -ue
DN="$(readlink -f "$(dirname "${0}")")"
. "${DN}"/../etc/path.sh
. "${DN}"/../lib/libstr
infoh "YuZJLab LibDO Manager"
infoh "Copyright (C) 2020 YU Zhejian"
[ "${mypython}" != "ylukh" ] && "${mypython}" "${DN}"/exec/libdoman.py "${PWD}" "${@}" || . "${DN}"/exec/libdoman.sh
