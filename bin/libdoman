#!/usr/bin/env bash
# LIBDOMAN V5
DN=$(dirname ${0})
echo -e "\e[33mYuZJLab LibDO Manager."
echo -e "Copyright (C) 2020 YU Zhejian\e[0m"
mypy=$(cat ${DN}/../etc/python.conf)
if [ -x "${mypy}" ];then
    ${mypy} ${DN}/exec/libdoman.py "${@}"
else
   . ${DN}/exec/libdoman.sh "${@}"
fi
